# === 数据库配置 ===
DATABASE_URL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# === 调度配置（默认每日凌晨3点执行） ===
SCHEDULE_CRON: "0 3 * * *"

# === 爬虫行为配置 ===
# 并发请求数
CONCURRENCY: 5
# 请求间隔（秒）
REQUEST_DELAY: 2
# 是否启用 StealthyFetcher 绕过反爬
USE_STEALTH: true
# 是否遵守 robots.txt
RESPECT_ROBOTS: true
